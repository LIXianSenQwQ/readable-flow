# Readable Flow 中文版本（非默认入口）

> 默认执行入口是 `SKILL.md`。本文件保留中文版本，方便人工阅读、复核和后续迭代。

## 核心目标

优先生成让人能读懂、能维护的代码。本 skill 不是反对封装，而是反对 AI 为了“看起来工程化”把一个业务流程拆散到许多小方法、helper、manager、processor、接口或抽象层里。

优先选择一个清晰主流程，而不是一堆浅包装。允许“大方法”，前提是它能从上到下表达业务流程，并且用清楚的步骤注释分段。不要因为方法看起来长就自动拆分。

## 单入口线性流程

按以下顺序执行：

1. 读取本地上下文。
2. 为每个业务用例选择一个主入口方法。
3. 写出可读的线性主流程。
4. 在任何提取或抽象前执行可读性检查。
5. 通过可观察行为做测试和 review。

## 1. 读取本地上下文

设计、实现或 review 前，快速查看项目已有约定：

- `AGENTS.md` / `SKILL.md`
- `CONTEXT.md` / `CONTEXT-MAP.md`
- `docs/adr/`
- 附近的生产代码和测试

这些文件只用于校准术语、边界、风格和既有决策。不要因为启用本 skill 就主动创建或修改 `CONTEXT.md`、ADR 或其他文档。

## 2. 选择一个主入口方法

每个业务用例优先使用一个主方法承载完整、可读的流程。在 Spring 项目里，这通常是 controller、job、listener 或 application service 调用的 service 方法。

读者打开入口方法后，应该能理解：

- 校验了什么
- 读取了哪些数据
- 做了哪些业务判断
- 改变了哪些状态
- 返回或发布了什么结果

不要让读者为了理解一个用例，在许多一行私有方法之间来回跳转。

## 3. 写可读的线性流程

方法可以较长，但必须保持可读。用步骤注释把有意义的业务块分开。

中文 Java 项目优先使用这种注释：

```java
// 步骤1：校验输入参数，避免后续流程处理无效请求

// 步骤2：读取订单与用户信息，保证业务判断基于最新状态

// 步骤3：根据当前订单状态执行业务校验

// 步骤4：保存结果并返回调用方需要的响应
```

英文项目可使用等价形式：

```java
// Step 1: Validate input before the business flow starts

// Step 2: Load the data needed for business decisions

// Step 3: Apply the business rules in execution order

// Step 4: Persist the result and return the response
```

不可读的大方法仍然不合格：没有步骤结构、混杂无关职责、隐藏失败处理，或把大量分支堆成一坨代码，都需要整理。

## 4. 执行可读性检查

在提取 helper、小方法、接口、抽象类、抽象方法、模板方法、工厂、策略、适配器、基类、manager、processor、handler、executor、注册表或回调机制前，先回答：

1. 这个提取是否让主流程更容易从上到下阅读？
2. 现在是否已经有真实复用，而不是想象中的未来复用？
3. 提取后的名称是否表达业务步骤，而不是技术包装？
4. 维护者是否会因为这个提取更快理解用例？
5. 复杂性是否来自当前需求或附近代码，而不是凭空预留？
6. 行为测试是否仍然能描述功能，而不依赖这个内部结构？

默认判定：

- 只有一个实现时，不创建接口。
- 只有一个算法时，不创建策略。
- 只有一个调用方时，不创建框架或 manager 层。
- 只有一个可复用块时，除非明显提升可读性，否则保持内联。
- 只有一个步骤可变时，优先清晰的内联分支或小的私有方法，不直接上模板方法。
- 只为了未来扩展时，判定为不必要。

检查不通过时，保持内联或使用最简单的局部结构，并简短说明移除了哪类复杂性。例如：“保持内联，因为提取私有方法会隐藏支付流程，且没有带来复用。”

## 5. 通过行为测试和 review 验证

涉及业务逻辑时，使用行为优先的小竖切：

1. 写一个通过公开接口观察真实结果的测试。
2. 写能通过测试的最小可读流程。
3. 第一个行为变绿后，再添加下一个行为。
4. 只在测试为绿色时重构，并且只有在提升可读性时重构。

测试应该描述系统对调用方表现出什么行为，不要绑定私有方法名、类层次或临时抽象。

review 时优先指出：

- 主流程被拆散到过多小方法里。
- 长方法没有清晰步骤注释。
- helper 方法只包装一两行代码，没有业务含义。
- 接口、抽象类或策略只有一个真实实现。
- `Manager`、`Processor`、`Handler`、`Helper`、`Util` 这类命名隐藏了领域意图。
- 读者无法从入口方法理解完整用例。

给出具体简化建议。优先说：“把这两个私有方法内联，并在校验和保存块前增加步骤注释。”避免只说：“这里可能过度设计。”
